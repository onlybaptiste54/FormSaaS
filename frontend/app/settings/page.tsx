"use client";

import { FormEvent, useEffect, useState } from "react";
import { Building2, Check, Palette, Save, ShieldCheck } from "lucide-react";
import { PageHeader, Shell } from "@/components/shell";
import { Loading } from "@/components/ui";
import { api } from "@/lib/api";
import type { Me } from "@/lib/types";

export default function SettingsPage(){const[me,setMe]=useState<Me|null>(null);const[saved,setSaved]=useState(false);useEffect(()=>{api<Me>("/me").then(setMe)},[]);async function submit(e:FormEvent){e.preventDefault();if(!me)return;const updated=await api<Me>("/company",{method:"PATCH",body:JSON.stringify(me.company)});setMe(updated);setSaved(true);setTimeout(()=>setSaved(false),1600)}if(!me)return <Shell><Loading/></Shell>;const set=(key:string,value:string)=>setMe({...me,company:{...me.company,[key]:value}});return <Shell><div className="page-wrap settings-page"><PageHeader eyebrow="IDENTITÉ CLIENT" title="Paramètres" description="Ces informations personnalisent chaque campagne et ses mentions légales."/><form onSubmit={submit}><section className="panel settings-section"><div className="settings-heading"><div className="metric-icon"><Building2/></div><div><h2>Entreprise</h2><p>Les informations visibles sur vos formulaires.</p></div></div><div className="form-grid"><label>Nom commercial<input value={me.company.name} onChange={e=>set("name",e.target.value)}/></label><label>Secteur d’activité<input value={me.company.sector} onChange={e=>set("sector",e.target.value)}/></label><label>Nom légal<input value={me.company.legal_name} onChange={e=>set("legal_name",e.target.value)}/></label><label>Numéro SIRET<input value={me.company.siret} onChange={e=>set("siret",e.target.value)}/></label><label className="full">Adresse postale<input value={me.company.address} onChange={e=>set("address",e.target.value)}/></label></div></section><section className="panel settings-section"><div className="settings-heading"><div className="metric-icon"><Palette/></div><div><h2>Identité visuelle</h2><p>Une palette appliquée avec mesure à vos formulaires.</p></div></div><div className="color-grid"><label>Couleur principale<div className="color-field"><input type="color" value={me.company.primary_color} onChange={e=>set("primary_color",e.target.value)}/><input value={me.company.primary_color} onChange={e=>set("primary_color",e.target.value)}/></div></label><label>Couleur d’accent<div className="color-field"><input type="color" value={me.company.accent_color} onChange={e=>set("accent_color",e.target.value)}/><input value={me.company.accent_color} onChange={e=>set("accent_color",e.target.value)}/></div></label><label>Ton de voix<select value={me.company.tone} onChange={e=>set("tone",e.target.value)}><option>Professionnel</option><option>Décontracté</option><option>Premium</option></select></label></div></section><section className="panel settings-section"><div className="settings-heading"><div className="metric-icon"><ShieldCheck/></div><div><h2>Confidentialité & RGPD</h2><p>Le contact de référence pour les demandes liées aux données.</p></div></div><div className="form-grid"><label>Email DPO<input type="email" value={me.company.dpo_email} onChange={e=>set("dpo_email",e.target.value)}/></label></div></section><div className="save-bar"><span>{saved&&<><Check size={17}/>Modifications enregistrées</>}</span><button className="button button-primary">{saved?<Check size={17}/>:<Save size={17}/>}Enregistrer</button></div></form></div></Shell>}
